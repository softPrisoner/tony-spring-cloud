server:
  port: 9001

spring:
  application:
    name: eureka-user-service
eureka:
  instance:
    ip-address: localhost
    prefer-ip-address: true
    appname: eureka-user-service
  client:
    register-with-eureka: true
    fetch-registry: true
    region: CN
    service-url:
      defaultZone: http://localhost:8761/eureka/
